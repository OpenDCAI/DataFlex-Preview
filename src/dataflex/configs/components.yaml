# configs/components.yaml
selectors:
  less:
    name: less
    params:
      save_dir: ${output_dir}/less_output
      gradient_type: adamw
      proj_dim: 4096
      seed: 123

  loss:
    name: loss
    params:
      cache_dir: ../dataflex_cache
      focus: "medium"

  delta_loss:
    name: delta_loss
    params:
      cache_dir: ../dataflex_cache
      window_size: 0.2